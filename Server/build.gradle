plugins {
	id 'application'
}

version = '1.0'

repositories {
	mavenCentral()
}

dependencies {
	testImplementation libs.junit
}

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(25)
	}
}

jar {
    manifest {
        attributes(
            'Main-Class': 'Server'
        )
    }
}